// Normal development builds are unrestricted. Safe-mode builds can run
// `NEXTE_SAFE_MODE=1 scripts/build_hvigor_signed.sh` to temporarily flip this flag.
export const SAFE_MODE_BUILD_ENABLED: boolean = false
