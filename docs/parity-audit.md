# NextE ↔ eros_fe parity audit (2026-06-15)

## DONE so far (2026-06-15, device + contract verified)
**Gallery detail — full re-architecture (the central complaint):** section order now header → info → relations → tags → **COMMENTS peek (max 2)** → **PREVIEWS peek (first page)**; comments before previews; "view all" → new **GalleryComments** page; "more" → new **AllThumbnails** page (paged 4-col grid); previews no longer infinite-inline-load; **READ button moved into the header** beside the cover (no giant slab); nav-bar title is **scroll-driven** (no duplicate with the header card); tag **namespace HEADER** colored, chips neutral. Device-verified on 192.168.50.197.
**Correctness (contract-locked):** `advsearch=1` now sent so EH stops silently dropping the whole advanced-filter block; usertag parser captures EH text/border/fill colors plus default-vs-custom color state so My Tags chips are readable and future edit UI can preserve color semantics; list parser handles **expunged** rows (robust postTime through a `<s>` wrap + struck-through date on cards).

Remaining backlog below (the reader cluster is next; the heavy items there — ser-keyed image map for O(1) jump, native immersive chrome, persistent reading position — need focused work, not a rushed pass).

---

Source: 6-agent comprehensive audit (run wf_7c15c408-b0f). 83 findings (65 do-now, 18 deferred). Each line: severity · kind · title.
**Scope rule (standing):** READ-completeness / architecture / correctness / native-UI = do now. WRITE-ops (vote/reply/edit/compose/add-remove-move fav/usertag edit/tagset mgmt) + big new subsystems (tag-translation DB, quick-search profiles, image search, local favorites) = DEFER — marked ⏸.

## Gallery detail page

> NextE's gallery detail is roughly half-built against eros_fe's target and several of its core architectural decisions are inverted. The single biggest miss is SECTION ORDER + the previews model: NextE renders header → infoBar → relations → a giant full-width READ button → tags → PREVIEW GRID (with an inline "load more" that infinitely concatenates pages into one list) → comments LAST. eros_fe deliberately puts COMMENTS (a filtered peek of max 2) BEFORE the PREVIEWS (a peek of only the first page) precisely so comments are reachable without scrolling past thousands of thumbnails, and routes "al

- [ ] **HIGH** · _architecture_ · Section order inverted: PREVIEWS render before COMMENTS
  - FE: gallery_page_sliver.dart builds slivers in this exact order: header → GalleryActions → tags MiniTitle+tags → chapter → COMMENTS MiniTitle + TopCommentEx peek (lines 158-205) → first-page thumbs ThumbSliverGrid + MorePreviewButton (lines 206-234). Comments are placed deliberately BEFORE the thumbnails so they are reachable without scrolling past every preview; the previews are the LAST block and ar
  - NextE: GalleryDetailPage.ets build() order is: GalleryHeaderCard → GalleryInfoBar → relationsRow → readButton → GalleryTagsCard → GalleryPreviewGrid (with inline load-more) → GalleryCommentsCard LAST (lines 60-122). Comments come after the entire preview grid, so on any multi-page gallery the user must scr
  - Fix: Reorder the SecondaryListScaffold ListItems to header → infoBar → relations → tags → comments-peek → previews-peek (last). Keep each block a GroupedListSection card. The comments card must sit immediately after tags and before the preview card.
- [ ] **HIGH** · _architecture_ · Previews do infinite inline paging instead of a first-page peek + separate page
  - FE: eros_fe shows ONLY firstPageImage in the detail (gallery_page_sliver.dart ThumbSliverGrid images: pageState.firstPageImage) and a single MorePreviewButton whose onPressed routes to EHRoutes.galleryAllThumbnails (gallery_widget.dart MorePreviewButton lines 434-462). It NEVER appends more preview pages inline. setImageAfterRequest captures firstPageImage as a fixed slice (gallery_page_controller.dar
  - NextE: GalleryPreviewGrid has an inline 'load more' Button that calls onLoadMore → vm.loadMorePreviews(), which does `this.images = this.images.concat(more)` and increments loadedPreviewPages (GalleryDetailViewModel.ets lines 70-89), growing the in-page grid unboundedly. This is exactly the 'infinite inlin
  - Fix: Cap the detail preview card to the first preview page only (the images returned by getGalleryDetail). Replace the inline load-more Button with a 'more thumbnails / no more thumbnails' entry row that pushPathByName('AllThumbnails', AllThumbnailsParams) to a NEW separate page. Remove loadMorePreviews/loadedPreviewPages/loadingMorePreviews from the detail VM (move paging to the AllThumbnails controll
- [ ] **HIGH** · _architecture_ · No separate AllThumbnails page (bidirectional paging + jump dialog + auto-position)
  - FE: all_thumbnails_page.dart + all_thumbnails_controller.dart implement a dedicated paged grid: fetchThumbnailsNext / fetchPreviewsPrevious (pull-to-refresh loads the PREVIOUS page), fetchThumbnailsFromPage(jump), a JumpDialog with a CupertinoSlider over maxpage (=filecount/firstPageLen+1), canShowJumpDialog gating, scrollToTop, and _autoJumpTo() which on open computes the row/offset from firstPageIma
  - NextE: No AllThumbnails page or route exists. AllThumbnailsParams is declared in RouteParams.ets (lines 64-86) but Index.ets routerMap only handles GalleryDetail/Reader/Download/EhLogin/MyTags — there is no 'AllThumbnails' case and no page file. None of bidirectional paging, jump dialog, auto-position, or 
  - Fix: Create feature/gallery AllThumbnailsPage.ets + AllThumbnailsViewModel on the shared PullRefreshGridScaffold: pull-refresh = load previous page, onReachEnd = load next page, a title-bar jump action opening an HDS dialog with a Slider (1..maxPage), and an onAppear auto-scroll that positions to the first unseen thumb (firstPageImage.length). Register an 'AllThumbnails' case in Index.routerMap. Tappin
- [ ] ⏸ **HIGH** · _architecture_ · No separate Comments page (composer + per-comment vote/translate/reply)
  - FE: comment_page.dart is a full CupertinoPageScaffold ('Gallery comments') with a pull-to-refresh comment list (filtered by uploader-only + block rules + score threshold) plus a bottom comment text field with send/edit/reply states (buildCommentTextField, _buildOriText). comment_item.dart in full (non-simple) mode renders score badge (tappable → scoreDetails dialog), translate button, edit button, vot
  - NextE: No Comments page or route exists. GalleryCommentsParams is declared in RouteParams.ets (lines 49-58) but no 'GalleryComments' route is registered and no page file exists. GalleryCommentsCard is the only comment UI and it is read-only: author + time + score + plain text with a URL data-detector. EhGa
  - Fix: Create feature/gallery CommentsPage.ets on PullRefreshListScaffold and register a 'GalleryComments' route. For read-completeness parity render score-detail on tap, reply context, and richer comment body; defer the composer/vote/reply WRITE actions to the write-ops milestone but structure the page to receive them. Extend EhGalleryComment + EhCommentParser to capture memberId, vote/canVote/canEdit f
- [ ] **HIGH** · _missed-logic_ · Comment peek has no filtering and shows ALL comments inline
  - FE: TopCommentEx (gallery_widget.dart lines 342-417) shows at most max=2 comments after applying: (1) showOnlyUploaderComment → keep only the uploader's comments (matched by the score-empty memberId, fallback to uploader name); (2) else filterCommentsByScore → drop comments whose score ≤ scoreFilteringThreshold; (3) block-list rules on both comment text and commentator name; then take(2); and renders 
  - NextE: GalleryDetailPage passes GalleryCommentsCard with the DEFAULT max=0, which the card treats as 'show them all' (shown() returns the full list when max<=0, GalleryCommentsCard.ets lines 20-24). So the detail page renders every comment with no uploader/score/block filtering and no 2-item cap. The card 
  - Fix: Pass max:2 and onMore (pushPathByName 'GalleryComments') to GalleryCommentsCard on the detail page. Port the three filters (showOnlyUploaderComment, score threshold, block-list) into a shared helper applied before the peek and on the full page. Add the corresponding settings (showOnlyUploaderComment / filterCommentsByScore / scoreFilteringThreshold) — none exist in NextE today.
- [ ] **HIGH** · _ui-native_ · READ button is a giant full-width capsule, not in the header beside the cover
  - FE: header.dart places ReadButton and GalleryFavButton in a Row inside the header's right column, beside the cover (lines 73-82): a compact CupertinoButton.filled (minSize 24, horizontal 18) reading 'READ' or 'READ {lastIndex+1}'. The read action belongs to the header card, balanced against the fav button on the same row.
  - NextE: GalleryDetailPage.readButton() is a separate full-width ListItem: Button(...).width('100%').height(BUTTON_HEIGHT) (lines 188-202), a giant bar between relations and tags. GalleryHeaderCard's own comment even notes 'the read button moves into this header in a follow-up' — it hasn't. There is no fav b
  - Fix: Move a compact read button (Capsule, auto width) into GalleryHeaderCard's right column, on a Row at the bottom with the fav button (use an HDS button + SymbolGlyph heart for fav). Remove the full-width readButton ListItem. Show 'READ {n}' when there's reading progress (requires the reading-progress state below).
- [ ] **MEDIUM** · _ui-native_ · Nav title permanently duplicates the header card title (no scroll-driven reveal)
  - FE: gallery_page_sliver.dart's CupertinoSliverNavigationBar keeps the cover thumbnail + read button in the nav bar HIDDEN until scrolled (AnimatedOpacity driven by hideNavigationBtn, toggled by scrollControllerLister at kHeaderHeightOffset). The largeTitle is the italic SUBTITLE, and the main title is NOT shown as a permanent nav middle — the header card owns the title until you scroll past it.
  - NextE: GalleryDetailPage sets .titleBar({ content: { title: { mainTitle: this.navTitle() } } }) where navTitle() returns the gallery title (lines 35-40, 125). So the HDS title bar always shows the full gallery title AND GalleryHeaderCard renders the same title — a permanent duplicate, and the title bar nev
  - Fix: Use V2Next's TopicDetail pattern: keep the title bar mini/empty (or a neutral 'Gallery' label) until the header scrolls out of view, then reveal the gallery title (drive via onDidScroll offset threshold → @Local showNavTitle, publish into the HDS title bar). Mirror eros_fe by optionally surfacing the cover thumbnail in the bar on scroll.
- [ ] **MEDIUM** · _missed-logic_ · Missing getMoreGalleryInfoOne fallback when filecount is absent
  - FE: _fetchData (gallery_page_controller.dart lines 153-161) checks `if galleryProvider.filecount == null || isEmpty` and calls Api.getMoreGalleryInfoOne(provider) to backfill metadata (via /api.php gdata) BEFORE the detail fetch, so deep-link / sparse seeds get a full header.
  - NextE: GalleryDetailViewModel.load() goes straight to getGalleryDetail with no filecount check and no gdata backfill (lines 37-63). A deep-linked gallery with no list seed gets only whatever the detail HTML parse yields; there is no getMoreGalleryInfoOne equivalent call path in the detail flow.
  - Fix: In load(), when the seed has no fileCount, call the /api.php gdata batch (EhApiPhpService) for this gid/token first and merge it before getGalleryDetail, matching eros_fe's enrich-then-detail order.
- [x] **MEDIUM** · _missed-logic_ · No rating inheritance on non-refresh (list-seed colorRating/ratingCount dropped) — fixed 2026-06-19 in `fix(gallery): preserve seed rating on detail open`
  - FE: _fetchData captures _oriColorRating/_oriRatingCount/_oriRatingFallBack before the detail fetch and, when NOT refreshing, re-applies them: colorRating from the original seed, ratingCount/ratingFallBack falling back to the seed (gallery_page_controller.dart lines 168-220). The detail page's rating block is often less precise than the list seed's color-rated value, so the seed wins on first load.
  - NextE: GalleryDetailViewModel.load() does `this.gallery = this.gallery.merge(res.gallery)` unconditionally (line 51). EhGallery.merge lets any non-empty/non-zero detail value overwrite the seed (rating>0, colorRating non-empty, ratingCount non-empty — lines 165-169), so the detail parse clobbers the list-s
  - Fix: Add a non-refresh inheritance step: before merge, snapshot seed colorRating/ratingCount/ratingFallBack; after merge, if not a refresh, restore colorRating from the seed and keep ratingCount/ratingFallBack when the detail value is weaker (mirror eros_fe). Thread a refresh flag through load().
  - Current: `GalleryDetailViewModel.fetchAndApply(..., refresh)` now snapshots seed `ratingFallBack` / `ratingCount` / `colorRating` after optional gdata enrich and before detail fetch. Initial opens preserve seed `colorRating` and use seed count/display rating only when detail is weak; pull-to-refresh passes `refresh=true` and trusts fresh detail. Locked by `scripts/test_gallery_merge_contract.mjs`; signed build + Mate X7 emulator detail smoke evidence: `/private/tmp/nexte_detail_rating_inheritance_evidence/`.
- [ ] **MEDIUM** · _missed-logic_ · No 5s-delayed history add
  - FE: Both the cache path (_loadDataFromCache lines 278-283) and the fresh-fetch path (_fetchData lines 240-245) schedule `Future.delayed(5000ms).then(addHistory(provider))` — history is recorded only if the user lingers ~5s, avoiding polluting history with quick back-outs.
  - NextE: There is no history subsystem touched by the gallery feature at all — grep for addHistory/lastViewTime/History in feature/gallery and shared/state finds nothing relevant. Opening a gallery never records view history (delayed or otherwise).
  - Fix: Add a history holder (AppStorageV2 + preferences) and, in onReady/load, schedule a 5s delayed add of the resolved gallery (cancel on early page disposal). Reuse V2Next's viewed-history pattern (ViewedTopics) for the native storage shape.
- [ ] **MEDIUM** · _missed-logic_ · No per-page thumb load lock + dedup map + no-cache-after-refresh
  - FE: loadImagesForSer computes the page from ser, stores an in-flight Future in mapLoadImagesForSer keyed by page (dedup), awaits it inside imageLoadLock.synchronized (single-flight), removes the entry on completion, and passes refresh:isRefresh so that AFTER a gallery refresh thumbnails are NOT read from cache — critical because changing images-per-page would otherwise corrupt cached pages (gallery_pa
  - NextE: GalleryDetailViewModel.loadMorePreviews just concatenates with no lock, no per-page dedup map, and no refresh/no-cache flag (lines 70-89). There is no loadImagesForSer (jump-to-arbitrary-page) at all, and no protection against double-triggered or out-of-order page loads or stale cache after a refres
  - Fix: In the AllThumbnails controller, port the lock + per-page Promise dedup map (Map<page, Promise>) for both next/previous/jump, merge into an images map keyed by page (no dupes), and pass a no-cache flag on loads issued after a refresh so re-paged thumbs aren't served from a stale cache.
- [ ] ⏸ **MEDIUM** · _shallow_ · Tag chips are not tappable to search and lack translate / vote coloring
  - FE: TagGroupItem/TagButton (gallery_widget.dart lines 466-610): each tag chip onTap runs a search `type:title` (NavigatorUtil.goSearchPageWithParam) and onLongPress opens a tag-info dialog; chip text honors isTagTranslate, and chip text COLOR reflects the user's vote (tagUpColor / tagDownColor) while the chip background is the tag's own color. The namespace header is colored.
  - NextE: GalleryTagsCard renders neutral, non-interactive chips: plain Text with no onClick search, no long-press tag-info, no translation toggle, and no vote-state text coloring (GalleryTagsCard.ets lines 39-53). Only the namespace header coloring is implemented (which is the correct eros_fe choice for the 
  - Fix: Make each chip tap publish a `type:tag` query to the shared search bus (like openSimilar) and switch to the Search tab; add a long-press tag-info dialog. Keep members neutral-background per eros_fe but apply vote-state text color once tag voting exists. Honor a tag-translate setting on display().
- [ ] ⏸ **LOW** · _missed-logic_ · No showKey / changeSource image resolution carried on the detail state
  - FE: fetchAndParserImageInfo resolves each page's full image and, when the parsed image returns a new showKey, writes it back to galleryProvider.showKey for the fast /api.php showpage path; uptImageBySer also promotes showKey (gallery_page_controller.dart lines 322-344, 553-642). changeSource clears the dio cache for the href and forces a re-fetch (nl source rotation).
  - NextE: EhGallery has a showKey field and EhGalleryImage has showKey/imageUrl/originImageUrl, but the detail VM never resolves images or propagates a showKey — getGalleryDetail returns previews only and load() does not seed/track a gallery-level showKey for the reader, nor is there any changeSource (source-
  - Fix: When the reader resolves a page and discovers a showKey, write it back to the shared gallery state so subsequent resolves use the fast path; expose a changeSource action (clear cached resolve + re-fetch with refresh) from the thumbnail long-press / reader. Keep this in the reader/resolve service but ensure the detail gallery object is the showKey owner.
- [ ] **LOW** · _missed-logic_ · Main/sub-title is not setting-driven (jpnTitleInGalleryPage)
  - FE: gallery_page_state.dart mainTitle/subTitle (lines 96-146) swap which of english/japanese is primary based on _ehSettingService.jpnTitleInGalleryPage: when the setting is on and a JP title exists, JP is the main title and EN becomes the subtitle (shown as the italic largeTitle in the nav bar); otherwise EN is main and JP is the sub.
  - NextE: GalleryHeaderCard always renders gallery.title() (EN-first, JP fallback) as primary and gallery.japaneseTitle as a secondary line (lines 26-46), with no setting to invert. EhGallery.title() is hard-coded english-first (model lines 82-84). There is no jpnTitleInGalleryPage setting in NextE.
  - Fix: Add a 'Japanese title in gallery page' reading setting; compute mainTitle/subTitle from it (JP primary + EN sub when on). Use the subtitle as the scroll-revealed italic nav subtitle (mirrors eros_fe largeTitle) rather than a second header line.
- [x] ⏸ **LOW** · _missed-logic_ · Read button shows no reading-progress page number (no lastIndex)
  - FE: ReadButton (gallery_widget.dart lines 228-264) shows 'READ {lastIndex+1}' when lastIndex>0 and opens the reader at lastIndex; lastIndex is streamed from the gallery cache (gallery_page_controller.dart lines 98-101 listenGalleryCache → lastIndex) and the jumpSer dialog (startReadDialog) offers to resume at a specific page.
  - NextE: Implemented via `GalleryReadProgressState` / `GalleryReadProgressSettings`, restored in `SettingsBootstrap`, written from `ReaderPage` on `vm.currentIndex` changes and flushed on close. `GalleryDetailPage` reads the saved index through `resumeIndex()` and labels the FAB with `继续 P{n}`.
  - Validation: existing intake evidence shows Mate X7 detail FAB `继续 P2` opening Reader at `2 / 138`; reader auto-read/error/loading lanes also verified resume-labeled entry points on current main.
- [x] **LOW** · _missed-logic_ · Preview/thumbnail CSS-sprite (gdtm) layout unsupported
  - FE: thumb_box.dart crops sprite-sheet previews via sourceRect = Rect.fromLTWH(offSet+1, 1, thumbWidth-2, thumbHeight-2) and aspect-ratio from thumbWidth/thumbHeight, supporting both largeThumb (one image per page) and the older small-thumb sprite layout where many previews share one image with a horizontal offset (lines 39-167).
  - NextE: Implemented in `EhGalleryImageParser` + `EhSpriteThumbnail`: unified `<a>` previews, legacy `gdtm` sprite previews, and whole-image fallback dimensions are now covered by `scripts/test_gallery_detail_parser_contract.mjs`.
  - Validation: `node scripts/test_gallery_detail_parser_contract.mjs`; signed build; emulator detail smoke evidence in `/private/tmp/nexte_gallery_image_parser_legacy_evidence/`.
- [ ] **LOW** · _shallow_ · Uploader / category are non-interactive (no uploader search, no category color in header)
  - FE: GalleryUploader (gallery_widget.dart lines 193-226) is tappable → search `uploader:{name}`; GalleryCategory (lines 267-306) renders a colored category pill. _CommentUser names are also tappable → uploader search.
  - NextE: GalleryHeaderCard renders uploader as plain non-tappable Text (lines 47-54). The category pill exists in GalleryInfoBar (colored, correct) but the uploader has no uploader-search affordance and comment author names in GalleryCommentsCard are static Text (not tappable to search).
  - Fix: Make the header uploader Text and comment author names onClick publish an `uploader:{name}` query to the search bus and switch tabs (reuse the openSimilar plumbing).
- [ ] **LOW** · _missed-logic_ · Title-bar trailing actions (refresh / addTag / share) missing
  - FE: gallery_page_sliver.dart's nav bar trailing has addTag (tags icon → add-tag flow), share (builds {base}/g/{gid}/{token} and shares), and on desktop a refresh button; the page is also pull-to-refreshable (EhCupertinoSliverRefreshControl → handOnRefresh).
  - NextE: GalleryDetailPage's HdsNavDestination title bar carries no trailing actions — no share, no add-tag, no refresh. The page is not pull-to-refresh (SecondaryListScaffold has no refresh wired; load() early-returns once loaded with no re-fetch path). The relationsRow has 'Similar/Parent/Torrents' but non
  - Fix: Add HDS title-bar menu/trailing buttons for share (systemShare with the /g/ URL, as V2Next TopicDetail does) and add-tag; add a refresh entry that re-runs load(refresh:true). Consider hosting the detail body on a pull-refresh-capable scaffold so refresh re-fetches detail + resets preview paging.

## Reader (image viewer)

> NextE's reader is a thin first-pass that implements only the skeleton of eros_fe's reader: three reading directions (LTR/RTL/vertical), a page-count + slider chrome, sequential preview-page loading, and per-image pinch/double-tap/pan zoom. It is far from parity. The single most important carefully-designed behavior — keep/restore reading position (eros_fe's per-gid lastIndex, debounced-saved on every page change and on close, surfaced as "READ {n}" on the detail button and resumed on open) — is entirely absent: NextE always opens at index 0 and never persists progress. The image-resolution pip

- [x] **HIGH** · _missed-logic_ · Reading position (lastIndex) is never saved or restored — the reader always opens at page 1
  - FE: eros_fe persists per-gallery reading progress. view_state.dart ctor wires `ever(_currentItemIndex, (val)=>saveLastIndex())` + `debounce(_currentItemIndex, ...saveToStore:true, 3.seconds)`; `saveLastIndex()` writes `pageState.lastIndex` and `_galleryCacheController.setIndex(gid, currentItemIndex, saveToStore)`. view_controller.onClose() calls `vState.saveLastIndex(saveToStore:true)`. onInit jumps t
  - NextE: Implemented. `GalleryReadProgressState` stores gid→index in AppStorageV2, `GalleryReadProgressSettings` persists JSON `{g,i,t}` records with a 3s debounce and close flush, `ReaderPage.onPageChanged()` writes the live index, and `GalleryDetailPage.openReader(this.resumeIndex())` starts from saved progress.
  - Validation: deterministic coverage exists through reader/FAB contracts and current intake evidence; Mate X7 evidence under `/private/tmp/nexte_detail_primary_fab_acceptance/`, `/private/tmp/nexte_reader_auto_read_evidence/`, and `/private/tmp/nexte_reader_loading_stage_acceptance/` shows resume labels and resumed Reader starts.
- [ ] **HIGH** · _missed-logic_ · showKey fast-path (/api.php method=showpage) is parsed but never used — every page does a full /s/ HTML GET
  - FE: eros_fe resolves the FIRST page via /s/ HTML to obtain the gallery showKey, then resolves all later pages via the cheap POST /api.php `method=showpage` (api.dart paraImageLageInfoFromApi L466-514: builds {method:'showpage', gid, page, imgkey, showkey}). fetchImage (view_controller.dart L590-625) explicitly orders the work around `needShowKey`: fetchAndParserImageInfo first when showKey is empty (t
  - NextE: ImageResolveService.resolve ALWAYS does `EhHttpClient.getText(image.sUrl)` (the /s/ HTML page) and scrapes <img id=img>. It captures r.showKey onto the image but never feeds it into a showpage API call. The service's own doc comment admits 'The fast path (POST /api.php method=showpage with the scrap
  - Fix: Add `EhApiPhpService.showPage(gid, page, imgkey, showKey)` (POST /api.php) and have ImageResolveService learn the showKey from the first /s/ resolve, store it on the VM (gallery-scoped), then resolve subsequent pages via showPage, falling back to /s/ HTML when showKey is empty/expired. Back it with a fixture contract test (scripts/test_image_page_api_contract.mjs) parsing the i3/x/y JSON fields.
- [ ] **HIGH** · _missed-logic_ · No 换源 / re-source recovery: the nl reload key is parsed but discarded, and there is no retry-with-source-switch
  - FE: EH serves images from rotating sources; a broken/expired token needs re-sourcing via the `nl(...)` reload key passed as `nl=sourceId` on the next request. eros_fe parses it (sourceId), exposes a long-press action sheet (view_image.dart L356-378 → `controller.reloadImage(ser, changeSource:true)`), and reloadImage (view_controller.dart L686-717) clears the disk/mem cache, sets changeSource, and re-f
  - NextE: EhImagePageParser.parse extracts `reloadKey` (the nl value) into ImagePageResult, but ImageResolveService.resolve never reads it and EhGalleryImage doesn't even carry a reloadKey/sourceId field. ReaderImagePage on failure only offers a single 'retry' button that re-runs the exact same resolve (same 
  - Fix: Add `reloadKey`/`sourceId` to EhGalleryImage; have ImageResolveService accept a `changeSource` flag that appends `?nl={reloadKey}` (and clears any cached entry) to re-source. Wire ReaderImagePage failure + a long-press context menu (HDS) to a VM `reloadImage(page, changeSource:true)`. Add an automatic bounded retry (errCount map per page, ~3-5 attempts) before showing the manual retry affordance.
- [ ] **MEDIUM** · _missed-logic_ · Double-page spread (single / oddLeft / evenLeft) is entirely missing
  - FE: eros_fe supports a full double-page mode persisted as ViewColumnMode {single, oddLeft, evenLeft} (ehsetting_service L57). DoublePageView (view_page.dart L184-361) pairs two ViewImages side-by-side, computing each page's aspect ratio (from imageSizeMap/imageWidth/thumbWidth) to lay them out in a Row with AspectRatio, reverses for RTL, and handles odd/even leading offsets. switchColumnMode cycles th
  - NextE: NextE only renders one full image per Swiper page (ReaderPage.HorizontalReader → ReaderImagePage). No column mode, no pairing, no aspect-ratio layout, no toolbar control, no setting. On tablets / landscape this wastes half the screen and diverges from eros_fe behavior.
  - Fix: Add a `ReadColumnMode` setting (off/A/B) to ReadModeSettings/state. In the horizontal reader, when enabled, render a two-image Row inside each Swiper page driven by page-pair indices, sizing each via aspectRatio captured on Image.onComplete (reuse the ratio-capture pattern already in ReaderVerticalImage). Add an HDS toggle in the reader chrome. Disable in vertical mode (as eros_fe greys it out).
- [x] **MEDIUM** · _missed-logic_ · Auto-page-turn (timer + interval + load-gating + wakelock) is missing
  - FE: eros_fe has a complete auto-read subsystem: tapAutoRead (view_controller L1034) toggles an interval Timer.periodic(turnPageInv) that advances pages (_autoTunToPage L1241), but only when the next page's load-complete flag is set (loadCompleMap), otherwise it cancels and waits for onLoadCompleted (L1147) to restart — so it never skips an unloaded page. turnPageInv is chosen via a Cupertino picker (L
  - NextE: Implemented in `fix(reader): add auto-read page turn` lane. Reader bottom chrome now has a secondary clock control; active auto-read uses a persisted interval from Reader Settings, pauses while the target page is not preview/real-image ready, warms the target, and resumes after Image.onComplete or resolved image URL readiness. The reader already keeps the screen awake on entry and restores it on close.
  - Evidence: `scripts/test_reader_auto_read_contract.mjs`, official signed Hvigor build, Mate X7 emulator `127.0.0.1:5555` screenshots in `/private/tmp/nexte_reader_auto_read_evidence/`: `chrome_before.png` shows the clock control at page `3 / 138`, `after_fixed.png` shows active auto-read advanced to `10 / 138`, and `settings.png` shows `自动翻页间隔 3s`.
- [ ] **MEDIUM** · _architecture_ · No reader settings page — direction is the only configurable option
  - FE: eros_fe has a dedicated ReadSettingPage (read_setting_page.dart) reachable from the reader's top-bar ellipsis (view_widget L996-1003 → EHRoutes.readSetting). It exposes: reading direction, screen orientation, double-page mode (with A/B preview), show-page-interval (gap between pages), turn-page animation toggle, volume-key paging (Android), fullscreen/immersive toggle, page-view engine, and slide-
  - NextE: NextE has only ReadModeSettings (direction) and ReadModeState. There is no reader settings page, no ellipsis/more entry in the chrome, and none of: orientation lock, page spacing, turn animation, volume keys, fullscreen toggle, preload count. The user cannot configure the reader beyond cycling direc
  - Fix: Build a ReaderSettingsPage using the shared settings scaffolds (mirror V2Next's settings list components) with: reading direction, double-page mode, screen orientation lock, page spacing, keep-screen-on, volume-key paging, preload count, tap-zone navigation toggle. Persist via a ReaderSettings preferences bundle + AppStorageV2 holders, live-applied. Add an HDS 'more' affordance in the immersive to
- [ ] **MEDIUM** · _ui-native_ · Reader chrome is hand-rolled scrim + Slider instead of native HDS immersive glass / V2Next overlay patterns
  - FE: The quality bar (project doctrine + task): the reader must be NATIVE-forward on API 26 (immersive glass/light-sensing chrome), taking UI experience from V2Next's ImagePreviewPage — overlay controls in dark translucent glass circles (Button type:Circle, '#66000000', SymbolGlyph white), safe-area-aware positioning (layout.bottomAvoidHeight), backdrop fade, springMotion animations — NOT a copy of ero
  - NextE: ReaderChrome (ReaderPage.ets L208-272) is a single hand-built Column with `OVERLAY_SCRIM` background, a plain page-counter Text, a bordered-Text mode toggle, and a raw Slider — no glass/blur, no top bar, no toolbar of actions, no SymbolGlyph icons, no light-sensing. It is neither eros_fe's bar set n
  - Fix: Rebuild the chrome with HDS immersive components + effectKit/backgroundBlurStyle glass panels: a top bar (back + title + share + more) and a bottom panel (slider + control row: direction, double-page, auto-read, thumbnails). Use SymbolGlyph icons (tintable — see the glyph-tint memory), '#66000000'-style translucent circular buttons like V2Next ImagePreviewPage, springMotion show/hide, and layout.b
- [ ] **MEDIUM** · _shallow_ · Per-image zoom is a weaker reimplementation of the transform math V2Next already ships — should reuse the shared coordinator
  - FE: V2Next ImagePreviewPage implements a correct, focal-point-aware zoom: center-pivot scale()+translate() with a fit-scale coordinator (ImagePreviewCoordinator.fitScale/clampOffset/clampScale), pinch that keeps the start focal point stationary ((1-k) focal correction, lines 472-493), double-tap toward the tapped point (applyDoubleTap L161-174), springMotion snap-back, and clamped pan bounds computed 
  - NextE: ReaderImagePage (ReaderPage.ets L284-427) re-rolls zoom from scratch and worse: double-tap zooms to a fixed 2.5 about the CENTER (no focal point — onDoubleTap L345 ignores tap location), pinch uses raw `baseZoom * e.scale` with no focal correction (image jumps under the fingers), pan is vertical-onl
  - Fix: Promote ImagePreviewCoordinator (or an equivalent) into NextE shared and have ReaderImagePage reuse it: focal-point pinch, double-tap-toward-point, fitted-image pan clamps, springMotion snap-back, all-direction pan when zoomed. This both fixes correctness and removes a parallel implementation.
- [ ] **MEDIUM** · _missed-logic_ · No tap-zone navigation (left/right thirds + top/bottom fifths + center toggle)
  - FE: eros_fe ImageGestureDetector (view_page.dart L413-456) divides the screen into zones: dx<1/3 → tapLeft (prev or next per direction), dx>2/3 → tapRight, and in the center column dy<1/5 → toPrev, dy>4/5 → toNext, else center → toggle bars. tapLeft/tapRight respect reading direction (LTR vs RTL invert; vertical guards on isScrolling) (view_controller L850-916). This is the primary one-handed paging g
  - NextE: ReaderPage's Swiper/List onClick toggles chrome for ANY tap anywhere (HorizontalReader.onClick L154, VerticalReader.onClick L179). There are no edge tap zones to page forward/back; the only paging is the swipe itself and the slider. No direction-aware left/right semantics.
  - Fix: Wrap the reader in a tap-region detector that maps left/right thirds to prev/next (direction-aware: invert for RTL), the center to chrome toggle, and (optionally) top/bottom strips to prev/next; in vertical mode page-scroll by a viewport. Keep it a single shared gesture helper so both readers share it.
- [ ] **MEDIUM** · _shallow_ · Jump-to-page from a far slider position loads pages one-by-one (O(n) sequential GETs) and blocks behind a spinner
  - FE: eros_fe loads a preview page on demand directly for the target ser: fetchThumb/loadImagesForSer (view_controller L481-499) fetches only the gallery preview page CONTAINING the requested ser, so a jump to page 380 fetches just that one preview page, then resolves the image. The slider jump (handOnSliderChangedEnd L950) immediately jumps the controller.
  - NextE: ReaderViewModel.jumpTo (ReaderViewModel.ets L96-105) calls ensureLoadedUpTo(index) which loops loadMore() — fetching EVERY intervening preview page sequentially (each a full /g/ ?p= GET) until images.length>index, showing a blocking 'jumping' spinner the whole time. Jumping near the end of a large g
  - Fix: Compute the preview page that contains the target index (index / pageSize) and fetch only that page (and fill the list sparsely or page-windowed), instead of loading all preceding pages. Resolve the target image, jump, and lazily backfill. This removes the O(n) jump cost and the long blocking spinner.
- [ ] **LOW** · _missed-logic_ · No thumbnail filmstrip with synchronized scroll + tap-to-jump
  - FE: eros_fe's bottom bar includes a horizontally-scrolling thumbnail strip (ThumbnailListView, view_widget L1371+) that stays synced to the current page (thumbScrollTo / syncThumbList, view_controller L1226-1239, L1424-1430), highlights the current item, and lets you tap a thumb to jump (onTap → logic.jumpToPage). A toolbar icon toggles it and a sync toggle exists.
  - NextE: NextE has no thumbnail strip at all — only the slider. Quick visual scanning/seek to a remembered page is unavailable.
  - Fix: Add an optional thumbnail filmstrip to the bottom glass panel (horizontal List of preview thumbs, reusing the preview-thumb component), current-page highlight, tap-to-jump (calls the existing jumpToPage path), and sync-to-current-page scrolling. Gate it behind a chrome toggle as eros_fe does.
- [x] **LOW** · _missed-logic_ · No distinct 509/429 quota or content-hidden error states; generic failure only
  - FE: eros_fe renders source-accurate error widgets per page: ViewErr509 (image-quota exceeded), ViewErr429 (rate-limited road-barrier), ViewAD (content hidden by pHash/QR check), and a generic ViewError with the message + page number (view_widget L29-212). These tell the user WHY a page is blank (quota vs throttle vs filtered) and the failed branch even auto-retries (errCountMap).
  - NextE: `b7ef697 fix(reader): distinguish image failure states` adds ReaderImageFailureKind (`Generic` / `Quota` / `RateLimited`), classifies `image509` / 509 / 429 / throttling errors, and renders distinct title/hint text in the shared black-canvas ReaderFailureOverlay for both paged and vertical readers.
  - Evidence: `scripts/test_reader_error_state_contract.mjs`, `scripts/test_reader_failure_retry_ui_contract.mjs`, `scripts/test_image_resolve_showpage_contract.mjs`, V1 inventory 0, i18n parity, official signed Hvigor build, and Mate X7 emulator Reader smoke in `/private/tmp/nexte_reader_error_state_evidence/`. pHash/QR content-hide remains deferred.
- [ ] **LOW** · _missed-logic_ · No per-image save / share / original-image download from the reader
  - FE: eros_fe's reader top/bottom bars expose Save (tapSave → showSaveActionSheet) and Share (tapShare → showShareActionSheet) for the CURRENT page (view_controller L980-1032), plus a long-press image sheet offering save / share / re-source / open original (originImageUrl) (view_image L365-378). originImageUrl ('download original' /fullimg) is parsed and offered. V2Next ImagePreviewPage shows the native
  - NextE: NextE's reader has no save, no share, and no original-image download. EhGalleryImage carries originImageUrl and the parser fills it, but nothing in the reader uses it. There is no way to save the page you're reading.
  - Fix: Add Save (and optionally Share) controls to the reader chrome and a long-press menu, reusing V2Next's MediaLibraryKit save flow (showAssetsCreationDialog + copyFile) — ideally extract it from ImagePreviewPage into a shared ImageSaveService. Offer 'save original' when image.originImageUrl is present.
- [ ] **LOW** · _missed-logic_ · No screen-orientation control and no fullscreen/immersive enforcement
  - FE: eros_fe locks orientation per the orientation setting on reader open (view_controller onReady L252-259 sets preferred orientation; onClose L288 restores), exposing system/portraitUp/landscapeLeft/landscapeRight in the read settings. It also enters immersive fullscreen on open (setFullscreen → SystemUiMode.immersiveSticky, gated by viewFullscreen) and restores edgeToEdge on close (L755-767).
  - NextE: ReaderPage hides the title bar but does nothing about orientation (no lock option) and does not explicitly drive a window immersive/fullscreen mode on enter/exit — it relies on the default. No orientation setting exists.
  - Fix: On reader enter, optionally lock orientation via window.setPreferredOrientation per a new setting, and set the window to a true immersive layout (hide system bars / setWindowLayoutFullScreen) restoring on exit. Expose orientation (system/portrait/landscape) in the reader settings page.
- [ ] **LOW** · _shallow_ · Preload uses Image-component cachedCount only; no eros_fe-style N-ahead URL precache
  - FE: eros_fe precaches the next `preloadImage` pages ahead on every page change: ehPrecacheImages (common.dart L57-145) resolves each upcoming page's full-image URL (via showKey) AND precacheImage()s the bytes into the cache, gated by startPrecache/completeCache flags to avoid duplicate work; preloadImage is user-configurable (default 2). onReady seeds the initial precache and handOnPageChanged re-trig
  - NextE: NextE relies solely on Swiper/List `.cachedCount(2)` to keep neighbor components alive; each ReaderImagePage resolves its own URL only in aboutToAppear (when it enters the cache window). There is no explicit N-ahead URL resolution or byte precache, and no preload-count setting — so the next page's /
  - Fix: Add an explicit preload step in the VM: on currentIndex change, resolve (and warm) the next preloadCount pages' image URLs ahead of time (dedup via per-page state flags), so the Image component has a ready URL before the page is shown. Make preloadCount a reader setting. Combine with the showKey fast-path so prefetch is cheap.
- [ ] **LOW** · _missed-logic_ · No volume-key page turning
  - FE: eros_fe lets hardware volume keys page (view_controller addVolumeKeydownListen L301-313: volume_down→toNext, volume_up→toPrev), gated by the volumnTurnPage setting, and pauses the listener while the read-settings sheet is open (view_widget L1000-1002).
  - NextE: No volume-key handling in NextE's reader.
  - Fix: If HarmonyOS exposes volume-key events to the foreground page (verify via the harmony-next skill), add an opt-in volume-key paging setting wired to next/prev. If the platform does not allow intercepting volume keys, record this as an intentional platform limitation rather than a silent gap.

## Home / gallery list (list card, grid card, multi-source)

> NextE's Home list is roughly 50% of eros_fe parity. The list-card render is a reasonable native-HDS adaptation and correctly ports several carefully-designed details (favNote-vs-uploader swap, favcat heart color via SymbolGlyph, category color badge, rating sprite math, language code). But it is missing or shallow on many of eros_fe's deliberate behaviors: (1) the GRID mode is architecturally wrong — eros_fe grid is a variable-height IMAGE waterfall with a category-colored rotated corner ribbon (lang code), while NextE renders a uniform fixed-cell info-card grid that defeats the entire point o

- [ ] **HIGH** · _ui-native_ · Grid mode is an info-card grid, not eros_fe's image waterfall with rotated category ribbon
  - FE: eros_fe's grid (lib/pages/item/gallery_item_flow.dart `GalleryItemFlow`) is a pure IMAGE tile: the cover fills the cell at its true aspect (`_getHeight()` scales imgHeight by cell width), wrapped in a ClipRRect with `RotatedCornerDecoration.withColor(color: catColor.withOpacity(0.8), badgeSize 28x22)` showing the language code (`galleryProvider.translated`) as a rotated corner ribbon. There is NO 
  - NextE: GalleryGridCard.ets renders a uniform-height info card: a fixed `aspectRatio(0.7)` cover plus 2-line title, a rating row, and a category-badge/page-count row, inside a `card_background` rounded card. PullRefreshGridScaffold.ets uses a `Grid` with `columnsTemplate('1fr 1fr')` and `rowsGap` — every ce
  - Fix: Replace the fixed-cell Grid with a native `WaterFlow` + `FlowItem` (ArkUI's masonry container) in a shared PullRefreshWaterFlowScaffold. Make GalleryGridCard a cover-only tile: `Image(...).objectFit(ImageFit.Cover)` sized to imgWidth/imgHeight aspect (fall back to 0.7) with no title/rating/category text. Render the lang code as a category-colored rotated corner badge — use an overlay `Text` rotate
- [ ] **HIGH** · _architecture_ · Toplist source completely missing (parser, paging model, and source entry)
  - FE: eros_fe ships toplist as a first-class source: tabhome_controller.dart registers `EHRoutes.toplist` (ToplistTab), toplist_controller.dart `TopListViewController` extends DefaultTabViewController and swaps in `ToplistFetchListClient`, and request.dart routes `GalleryListType.toplist` to `/toplist.php` with `tl=<11/12/13/15>` (all/year/month/yesterday) and crucially uses PAGE-NUMBER paging: `if (isT
  - NextE: Grep across feature/ and shared/ finds ZERO toplist references. GalleryListViewModel.source only handles ''/'popular'/'watched'; EhApiService.getGalleryList has no toplist path; EhGalleryListParser only extracts `next=`/`prev=` gid cursors (RE_NEXT/RE_PREV) and no page-number/maxPage parsing. The wh
  - Fix: Add a 'toplist' source: route `/toplist.php?tl=N&p=K` in EhApiService; extend GalleryList with `nextPage`/`prevPage`/`maxPage` and have EhGalleryListParser additionally parse `table.ptt` and the `searchnav` prev/next hrefs (the row layout is identical, so parseRow is reused). Drive paging by page index when nextGid is absent. Surface toplist in the Home SourceBar (or a dedicated HDS sub-entry) wit
- [ ] **HIGH** · _missed-logic_ · Parser never sets `expunged`; strikethrough-on-posted-time signal is dead
  - FE: eros_fe's gallery_list_parser.dart detects expunged galleries structurally: `if (elmPostTime?.children.isNotEmpty) expunged = true` (EH wraps the timestamp of a removed/expunged gallery in a child `<s>` element). The PostTime widget (gallery_item_flow.dart) then renders the date with `TextDecoration.lineThrough` when `expunged`. The WHY: a strikethrough date is EH's visual marker that the gallery 
  - NextE: EhGallery has an `expunged` field but EhGalleryListParser NEVER assigns it (grep: no `expunged` in the parser). Worse, RE_POSTED is `id="posted_\d+"[^>]*>([^<]+)<\/div>` which only matches a TEXT-only node — for an expunged row whose timestamp is wrapped in `<s>…</s>`, this captures nothing, so post
  - Fix: In parseRow, detect a child element inside the posted div (e.g. match `<s>` or any tag between `id="posted_…">` and `</div>`) → set `g.expunged = true`, and extract the inner text (strip tags) for postTime. Then in GalleryCard/GallerySimpleCard apply `.decoration({ type: this.gallery.expunged ? TextDecorationType.LineThrough : TextDecorationType.None })` to the postTime Text. Add a fixture row wit
- [x] **MEDIUM** · _missed-logic_ · List tags render monochrome — per-tag inline colors are discarded in the parser
  - FE: eros_fe's gallery_list_parser.dart reads each `div.gt`'s inline `style` and extracts color + background via `RegExp(r'#(\w{6})')` (matches[0] = text color, matches[3] = background color) into SimpleTag.color / backgrondColor; gallery_item.dart's TagItem then paints each tag with `ColorsUtil.getTagColor(_simpleTag.color)` / `backgroundColor`. The WHY: EH color-codes list tags by namespace/weight (e
  - NextE: Implemented in `d018a89 fix(list): parse inline tag colors`. `EhGalleryListParser` now parses `div.gt/gtl` open-tag attrs, preserves title/namespace, extracts the first and fourth `#rrggbb` values from `style`, and writes them into `SimpleTag.color/backgroundColor`. Existing `GalleryCard` chip rendering already uses those fields before falling back to neutral styling or usertag colors.
  - Evidence: `scripts/test_gallery_list_parser_contract.mjs` covers the eros_fe first/fourth-hex rule with a synthetic styled tag, plus neutral fallback for unstyled `gtl`; `scripts/test_usertag_enrich_contract.mjs` still protects usertag color precedence; official signed build passed. Mate X7 emulator homepage smoke installed the signed HAP and rendered real Home list cards normally; the reachable public sample had neutral tag chips, so visible colored-tag acceptance remains data-dependent.
- [ ] ⏸ **MEDIUM** · _missed-logic_ · Rating stars are never tinted by personal-vote color (colorRating ir/irr/irg/irb)
  - FE: eros_fe gallery_item.dart `_Rating` passes `colorLight: ThemeColors.colorRatingMap[colorRating]` to StaticRatingBar, where colorRatingMap maps `ir`→orange(default), `ir irr`→red, `ir irg`→green, `ir irb`→blue (theme_colors.dart). The list parser captures this class from `td.gl2c ... .attributes['class']` into `colorRating`/`isRatinged`. The WHY: a green/blue/red star color tells the user at a glan
  - NextE: EhGalleryListParser parses neither the `ir irX` class nor colorRating for list rows (it only computes the numeric rating from the sprite background-position; EhGallery.colorRating/isRated stay default). GalleryCard/GridCard/SimpleCard all call `Rating({ rating, indicator: true })` with the platform-
  - Fix: In parseRow, also capture the rating div's class (the `ir irX` variant) into `g.colorRating`/`g.isRated`. Since ArkUI's `Rating` can't recolor stars, render a small custom 5-star row (SymbolGlyph star.fill at sizes) tinted by a NextE `colorRatingMap` equivalent in EhConstants (orange default / red / green / blue), or overlay a tinted star strip. Keep it in a shared component reused by all three ca
- [ ] **MEDIUM** · _shallow_ · No fixed-height item mode with horizontal tag-waterfall; tags clipped to one row of 3
  - FE: eros_fe gallery_item.dart gates layout on `ehSettingService.fixedHeightOfListItems`: when on, the card is a fixed kFixedHeight(204), the title line count is `5 - tagLine` (computed from tag count / 4 in galleryitem_controller.tagLine), and tags render via `TagWaterfallFlowViewBox` — a HORIZONTAL multi-row waterfall (crossAxisCount = tagLine, height `tagLine*22-4`) that packs many tags compactly; w
  - NextE: GalleryCard has no fixed-height mode and no tag-waterfall: it always shows `simpleTags.slice(0,3)` in a single non-wrapping `Row` with `constraintSize({maxWidth:100})` each — only 3 tags, hard-clipped, with no relationship to `listViewTagLimit`, no tag-count-driven title line budget, and rows are va
  - Fix: Add the fixed-height list option (a setting like eros_fe's fixedHeightOfListItems). In fixed mode, constrain the card height and render tags in a wrapping container (Flex `wrap: Wrap` or a multi-row tag flow) honoring a tag limit, and budget title maxLines from tag-row count. Reuse a shared TagFlow component so list/detail share one tag renderer. Even without the setting, replace the slice(0,3) si
- [ ] ⏸ **MEDIUM** · _missed-logic_ · No per-item long-press action sheet (add/remove/change favorite, delete history)
  - FE: eros_fe galleryitem_controller.dart `onLongPress` → heavy haptic + `_showLongPressSheet`: a CupertinoActionSheet offering Add-to-favorites (when not favorited), Remove-from-favorites + Change-favorites (when favorited, via FavController.addFav/delFav and local `setFavTitleAndFavcat` optimistic update), and Delete (on history items). gallery_item.dart wires onTapDown/Up/Cancel for a press-color fee
  - NextE: HomePage's LazyForEach ListItem only wires `.onClick` (navigate to detail). GalleryCard/GallerySimpleCard have no long-press, no context menu, no press-state color. There is no list-level favorite/remove/change affordance at all — the user must open detail to favorite.
  - Fix: Add a long-press handler on the list row (HDS `bindContextMenu` or a gesture → HDS action sheet / Menu) offering Add/Remove/Change favorite (reusing the favorites slot picker) and, where applicable, history-delete. Apply the destructive-write guardrails from CLAUDE.md (dialog → confirm). Optimistically update EhGallery.favcat/favTitle so the heart recolors immediately (a single-writer command-bus 
- [ ] **MEDIUM** · _architecture_ · No jump-to-page / seek-by-date / load-previous paging — only forward cursor append
  - FE: eros_fe tabview_controller.dart builds a full paging toolkit: `showJumpDialog` switches between a page-number dialog (page-cursor lists like toplist, with `page_range 1 - maxPage` validation) and a jump/seek dialog (date `yyyy-MM-dd` / offset `\d+[wmy]` / GID, with a CupertinoDatePicker), plus `loadPrevious()` / `fetchPrevData()` (prev gid/page) and `keepPosition` so that after a jump the user can
  - NextE: GalleryListViewModel only supports forward `loadMore()` appending at `nextGid`. There is no prevGid usage, no loadPrevious, no jump-to-page, no seek-by-date, no maxPage, and no UI affordance for any of it. Pagination is strictly forward-infinite-scroll from page 1.
  - Fix: Extend the VM with prev-cursor / page-number support and a `jumpTo(date|offset|gid|page)` that resets the list to that anchor, plus an optional load-previous when anchored mid-list. Surface a jump entry in the HDS title bar (reuse V2Next's secondary-action affordances) with an HDS dialog: a number field for page-cursor sources and a date-picker + offset/GID field for gid-cursor sources, validating
- [ ] **LOW** · _missed-logic_ · loadMore lacks stale-cursor and duplicate-first-item guards
  - FE: eros_fe tabview_controller.dart `loadDataMore()` guards against re-fetching the same page: `if (lastNext == next) return;` (and stores `lastNext = next` before fetch), and only advances the cursor when the new page's first gid isn't already present (`state?.indexWhere(... == resultList.first.gid) == -1`). loadPrevious has the symmetric guard. The WHY: EH occasionally returns the same `next=` curso
  - NextE: GalleryListViewModel.loadMore only checks `isLoading || isLoadingMore || !hasMore`, then unconditionally `appendData(list.gallerys)` and sets `nextGid = list.nextGid`. If EH returns the same nextGid or overlapping rows, NextE appends duplicate gids (LazyForEach keyed by gid → key collision) with no 
  - Fix: Track `lastNext`; bail if the new cursor equals the last one. Before appendData, drop rows whose gid already exists in the data source (dedupe by gid). Set hasMore false when the page returns zero new rows. This avoids duplicate-key LazyForEach churn and tail loops.
- [ ] **LOW** · _missed-logic_ · Popular's no-paging contract is implicit/uncontrolled in the VM
  - FE: eros_fe treats popular as a fixed finite snapshot: request.dart `isPopular` strips jump/seek/page/next entirely and the popular controller is disabled/commented (no DefaultTabViewController paging). The WHY: `/popular` is a server-rendered fixed set with no pagination — requesting `next=` is meaningless.
  - NextE: EhApiService.getGalleryList does early-return the bare `/popular` snapshot (good), but GalleryListViewModel doesn't know popular has no pages: after the first load it leaves `hasMore = list.nextGid.length > 0`. Since the popular page has no `next=`, hasMore becomes false by accident rather than by c
  - Fix: In the VM, set `hasMore = false` explicitly when `source === 'popular'` (and skip loadMore for it) rather than relying on an absent cursor. This makes the no-paging contract intentional and prevents a stray loadMore during source transitions. Mirror eros_fe's explicit isPopular gating.
- [ ] **LOW** · _ui-native_ · Source switcher is reasonable but loses the favcat-bar pattern parity and toplist period selector
  - FE: eros_fe exposes front/popular/watched/toplist as discrete tabs (tabhome_controller.dart `tabPages`, custom_tabbar_page.dart) with per-source controllers, and toplist carries a period selector (toplist_controller `setToplist` → yesterday/month/year/all) in its nav bar. The WHY: each source has distinct paging/affordances, so eros_fe keeps them structurally separate with source-specific controls.
  - NextE: HomePage collapses front/popular/watched into inline scrolling chips in SourceBar (a fine native adaptation per the UI doctrine), but there is no toplist chip and thus no period selector, and the chips are hand-rolled Text pills (brand-color fill) rather than a shared HDS segmented/chip component. S
  - Fix: Keep the inline-chip adaptation (good native call) but add a toplist chip; when toplist is active, surface its period selector via an HDS popup/segmented control in the title bar. Promote the chip row to a shared, reusable selector component (the favorites favcat bar can share it). Use an HDS chip/segment style rather than ad-hoc brand-filled Text pills so the selected state matches platform conve

## Search

> NextE's Search is a thin slice of eros_fe's. The happy path works — title-bar HDS field, submit/refresh/loadMore via BasicDataSource, a filter sheet covering f_cats/f_sr/f_srdd/f_sp/f_spf/f_spt/f_sto/f_sh/f_sf*, and a recent-history empty state — and the native-UI direction (HDS title-bar field, PullRefreshListScaffold, bindSheet) is correct and should be kept. But it is missing entire sub-systems that define eros_fe's search experience: (1) tag autocomplete/suggestion (the `_delayedSearch` debounced flow that turns the page into a live namespace:tag picker via Api.tagsuggest + the local trans

- [ ] ⏸ **HIGH** · _missed-logic_ · Tag autocomplete / suggestion (live namespace:tag picker) is entirely missing
  - FE: search_page_controller.dart `_delayedSearch` (lines 159-237) is the heart of eros_fe search: a 500ms debounce on the text field that, while typing, sets `listType = ListType.tag` and queries suggestions for the LAST token (`currQryText = searchTextController.text.split(RegExp(r'[ ;"]')).last`, line 204). For zh locale it matches the local translation DB (`tagTransController.getTagTranslatesLike(te
  - NextE: GallerySearchPage has only two modes: history empty-state and results. There is no `listType=tag` mode, no debounced text monitoring, no call to any tagsuggest endpoint, no local tag-translation DB, and no suggestion list. Typing does nothing until the user hits Enter; there is no way to discover/co
  - Fix: Add a shared `EhApiPhpService.tagSuggest(text)` (POST /api.php {method:'tagsuggest',text}) returning {ns,tn} pairs into a SimpleTag-like model. Add a `@Trace suggestions` + debounced (~400ms) text monitor in SearchViewModel fed by SearchActionState.keyword (split last token on /[ ;"]/). Render suggestions as a third state in GallerySearchPage using an HDS list (e.g. HdsListItem rows with magnifier
- [ ] ⏸ **HIGH** · _architecture_ · Quick-search / saved-search profiles subsystem absent
  - FE: eros_fe has a full QuickSearchController (quicksearch_controller.dart) persisting a deduped `searchTextList` to the profile, with add/removeAt/removeAll and WebDAV sync (debounced). The search bar exposes it everywhere: SearchTextFieldIn shows a circle-plus to `addToQuickSearch` (search_page.dart 964-974, controller `addToQuickSearch` 313-318) and a list icon to open `quickSearchList` (controller 
  - NextE: No quick-search concept exists anywhere in NextE. No state holder, no settings persistence, no list page, no add/open affordances in the title-bar field or filter sheet. SearchTitleField exposes only the bare Search component (no plus/list trailing actions). Users cannot save or recall a query beyon
  - Fix: Add a shared `QuickSearchState` (@ObservedV2 @Trace items:string[]) + `QuickSearchSettings` (preferences-backed, dedup + cap, single writer) mirroring SearchHistorySettings. Add a QuickSearchPage in the user/search feature built on the shared list scaffold with HDS swipe-action delete + a clear-all title action (skip WebDAV/import-export for M-parity, defer). Surface 'save current query' and 'open
- [ ] **MEDIUM** · _missed-logic_ · Gallery-URL paste → jump-to-gallery detection missing
  - FE: search_page_controller `_delayedSearch` (188-202) checks if the typed text is a URL (`searchText.isURL && canLaunchUrlString`) and matches `regGalleryUrl`/`regGalleryPageUrl`; if so it sets `textIsGalleryUrl=true` and stores `_jumpToUrl`. SearchTextFieldIn then swaps the clear button for a circle-arrow-right that calls `jumpToGallery` → `NavigatorUtil.goGalleryPage(url:_jumpToUrl)` (controller 393
  - NextE: No URL detection in SearchViewModel or SearchTitleField. Pasting a /g/{gid}/{token} link and submitting runs it as a literal f_search keyword (which returns nothing useful). NextE already has EhUrlRouter.parseGallery used by deep links in Index/EntryAbility, but it is not wired into the search field
  - Fix: In SearchViewModel (or the page's submit handler), before running the remote search, test the trimmed query with the existing `EhUrlRouter.parseGallery`; if it resolves a GalleryRef, push 'GalleryDetail' (GalleryDetailParams(gid, token)) via connectNavStack instead of searching. Optionally show a 'go' affordance in the title-bar field's trailing area when the live text parses as a gallery URL (mir
- [x] **MEDIUM** · _missed-logic_ · Search-type segmented control (Gallery / Watched / Favorite) — implemented 2026-06-18 in `b3b74df feat(search): add scoped search filter`
  - FE: eros_fe's filter sheet (gallery_filter_view.dart 56-72, 137-153) shows a CupertinoSlidingSegmentedControl<SearchType> {normal, watched, favorite} that re-targets the same search box: SearchType maps to GalleryListType (controller `_typeMap` 239-246) so the query runs against `/`, `/watched`, or `/favorites.php`. The placeholder text changes accordingly (controller `placeholderText` 59-71). When se
  - NextE: SearchViewModel.buildQuery hardcodes `source:''` (front-page index only) — grep of feature/search shows no 'watched'/SearchType handling. The search field can only search the public index. There is no way to search within Watched or to scope to Favorites from the search tab. (EhApiService.getGallery
  - Fix: Added persistent `SearchFilterState.searchScope` with Gallery/Watched/Favorite choices at the top of `SearchFilterSheet`; Watched routes `SearchViewModel.buildQuery()` to `source:'watched'`; Favorite routes through `getFavoritesList(favcat:'a')`; route-param favorites search still keeps its specific favcat. Favorite scope hides the gallery-only category/rating/pages/options block. Validation: `test_search_scope_contract`, `test_search_filter_settings_contract`, `test_search_input_contract`, i18n parity, V1 inventory, official signed Hvigor build, and Mate X7 emulator UI evidence under `/private/tmp/nexte_search_scope_filter_evidence/`.
- [ ] **MEDIUM** · _shallow_ · Category-filter chip semantics inverted vs eros_fe (default-state + exclude logic mismatch)
  - FE: eros_fe stores `cat` == EH `f_cats` directly as the EXCLUDE bitmask. In GalleryCatFilter every category button DEFAULTS to ON/shown (filter.dart `value: _catMap[catName] ?? true`, 151), and toggling a button OFF adds its bit to f_cats (utility.dart convCatMapToNum: `if(!value) totCatNum += bit`, 598-606). Long-press inverts all others (filter.dart 161-176) — a fast 'only this category' gesture. So
  - NextE: SearchFilterSheet chips default UNSELECTED (selectedCats=0) and represent 'show'; fCats() does `ALL_CATS - selectedCats` (SearchFilterState 26-31) with a special-case that 0 selected == 0 excluded. Functionally the empty/full cases coincide, but the UX is inverted from eros_fe (eros_fe: everything l
  - Fix: Pick eros_fe's model to match user mental model: default all categories selected/lit, treat selectedCats as the SHOW set, keep fCats()=ALL-selected. Add a long-press 'solo this category' action. Render category chips with the per-category brand colors (port ThemeColors.catColor into EhSemanticColors) so cats are visually identifiable, consistent with the detail/list cards. Keep it in the HDS sheet
- [ ] **LOW** · _shallow_ · Min-rating filter limited to 2-5 toggle; no off-by-itself + UX mismatch
  - FE: eros_fe gates min-rating behind a switch (`searchWithMinRating` → f_sr=on) and only THEN reveals a 2/3/4/5-star segmented control (gallery_filter_view.dart 300-343), with an AnimatedCrossFade reveal. f_srdd is only sent when the switch is on (extension.dart 526-527). The two-step (enable, then choose) keeps the default state clean and matches EH's own form.
  - NextE: SearchFilterSheet uses a single RATINGS row [0,2,3,4,5] where 0 == 'Any' (SearchFilterSheet 20, 54-69); minRating>=2 is what triggers advsearch. This collapses enable+value into one row, which works but diverges from eros_fe's reveal pattern and offers no visual grouping. Minor, but the '0=Any' chip
  - Fix: Optional polish: model as an HDS toggle ('Minimum rating') that reveals a 2-5 star segmented control when on, matching eros_fe and EH's form. Keep the existing f_sr/f_srdd emission (already correct in EhApiService 117-120).
- [ ] **LOW** · _ui-native_ · Filter sheet is hand-rolled chip/toggle column, not HDS list components
  - FE: Per project doctrine (CLAUDE.md, MEMORY 'native HDS first'), settings-style option groups should use native HDS list/cell components (as V2Next does) rather than bespoke Text+Toggle rows. eros_fe's column is a limitation-driven Cupertino layout we should NOT pixel-copy; the WHY is the information grouping (Category / Rating / Pages / Options / Disable-default-filters sections), not the exact widge
  - NextE: SearchFilterSheet.ets builds raw `Text` section headers + bespoke chip `Text` pills + `Row`+`Toggle` ToggleRow + raw `TextInput` page boxes + capsule Reset/Apply buttons, all hand-laid with ThemeConstants spacing. It reads as a custom form, not the app's native settings idiom.
  - Fix: Rebuild the sheet body on the shared HDS list scaffolding used by settings sub-pages (grouped list sections with HDS cells: switch cells for the toggles, a stepper/segmented cell for rating, inline number cells for the page range). Keep category chips as a chip-group cell. Reuse the shared section-list component rather than ad-hoc Column+Text headers.
- [ ] **LOW** · _shallow_ · 'Disable default filters' collapses three independent EH params into one toggle
  - FE: eros_fe exposes f_sfl (language), f_sfu (uploader), f_sft (tags) as THREE independent switches under a 'Disable default filters' header (gallery_filter_view.dart 411-437; extension.dart 533-535). A user may want to ignore only their language default while keeping tag/uploader filters.
  - NextE: SearchFilterState.disableDefaultFilters is a single boolean that emits all three (f_sfl+f_sfu+f_sft) together (EhApiService 132-136; SearchFilterState comment 18-22 explicitly notes the grouping). Users lose the ability to disable them individually.
  - Fix: Split into three @Trace booleans (disableFilterLanguage/Uploader/Tags) emitted independently, rendered as three switch cells under one section header, matching eros_fe and the EH form exactly.
- [ ] ⏸ **LOW** · _missed-logic_ · Image search (search by file) feature missing
  - FE: search_page.dart trailing (809-821) has an image icon opening EHRoutes.searchImage, backed by search_image_controller.dart: pick an image (file picker + permission flow), POST it to EH's image-search endpoint (`searchImage(imagePath)`), and render results in the same gallery list. It is a first-class search affordance in eros_fe's search bar.
  - NextE: No image-search anywhere in NextE (grep of feature/search and shared shows nothing). The title-bar field has no image action and there is no controller/route.
  - Fix: Lower priority. When tackled: add a shared service that uploads a picked image (photoAccessHelper picker) to EH's lookup endpoint and parses the returned list, surfaced as a title-bar/sheet action that feeds the same SearchViewModel data source. Defer behind quick-search + autocomplete.
- [ ] **LOW** · _architecture_ · Advanced-search enable state + filter persistence not modeled as a profile
  - FE: eros_fe's AdvanceSearchController (advance_search_controller.dart) persists BOTH an `enableAdvance` flag and the full AdvanceSearch object into Global.profile via everProfile, so advanced settings survive restarts and are part of the sync-able profile. The filter view's 'Advanced Options' master switch + reset (gallery_filter_view.dart 173-203) toggles whether the advanced block is even sent (requ
  - NextE: SearchFilterState lives in AppStorageV2 (in-memory singleton) with NO preferences persistence — grep shows connectSearchFilter is only connected, never restored/saved by any *Settings bootstrap (unlike SearchHistorySettings which is restored in SettingsBootstrap). Filters reset on app restart. There
  - Fix: Back SearchFilterState with a SearchFilterSettings (preferences) restored in SettingsBootstrap.loadAll, mirroring SearchHistorySettings, so filters persist. Optionally add an explicit 'advanced options' enable toggle in the sheet that gates emission (matching eros_fe) instead of inferring from values.
- [ ] **LOW** · _missed-logic_ · Search submit ignores live keyword / can't browse by category-only (empty query)
  - FE: eros_fe's _startSearch trims the field and, when non-empty, searches; the cat/advanced filters are applied to the gallery index regardless. A user can also set f_cats with an empty search to browse a whole category. The search field and filters are decoupled from the keyword requirement.
  - NextE: SearchViewModel.search() and reapplyFilters() early-return when `this.query.length === 0` (SearchViewModel 49-52, 55-58). So applying filters (e.g. 'show only Doujinshi, 4★+') with an empty keyword does nothing — there is no category-only browse from the search tab. Also onFilterApply only re-runs i
  - Fix: Allow an empty-keyword search to run when filters are active (or a category is selected), so the filter sheet can drive a category/advanced browse like eros_fe. Guard the network call on (query.length>0 || filter.isActive()) rather than query.length>0 alone.
- [ ] ⏸ **LOW** · _shallow_ · Search history still lacks translated-history / append-compose parity
  - FE: eros_fe keeps up to 100 history entries (search_page_controller `_addHistory` 250-257), supports per-entry delete via long-press dialog (search_page.dart 447-487 with translation preview) AND a global clear, and a 'translate history' toggle that shows tag-translated subtitles for each chip (switchTranslateHistory + _searchHistoryBtnWithTranslate 490-559). History entries are tapped to APPEND to th
  - NextE: SearchHistorySettings now caps at 100 and history chips support long-press single-entry delete with toast. Chips still have no translated subtitle/toggle, and tapping a chip REPLACES the field + runs immediately (searchFromHistory sets keyword + runs) rather than appending to the current input.
  - Fix: Remaining parity is a translate-history toggle (depends on the tag-translation DB from the autocomplete finding) and changing chip tap to append-to-field (seed via seedSeq) so users can compose multi-tag queries, matching eros_fe's appendTextToSearch behavior.

## Favorites

> NextE's Favorites is a thin single-list-with-a-chip-bar reimplementation that captures only the happy path: load favcat 'a', toggle fav/posted order, infinite-scroll forward via nextGid. It is far from eros_fe parity. eros_fe models favorites as a first-class browsing surface — a per-favcat TAB BAR where every favcat is its own keep-alive sub-page with independent scroll/pagination state, a dedicated full-screen favcat SELECTOR page (with counts + colored hearts), a full favorites SEARCH surface (its own SearchType.favorite + sn/st/sf scope toggles), a jump-to-date/page (seek/jump) navigator w

- [ ] ⏸ **HIGH** · _missed-logic_ · Local favorites ('l' slot) completely missing — no LocalFavController, no persistence, no 'l' favcat
  - FE: eros_fe treats local favorites as a first-class favcat. lib/common/controller/localfav_controller.dart (LocalFavController extends ProfileController) holds RxList<GalleryProvider> loacalFavs persisted into Global.profile.localFav; addLocalFav/removeFav/removeFavByGid mutate it and everProfile writes it back. favorite_sel_controller.dart injects a synthetic Favcat(favId:'l', favTitle: local_favorit
  - NextE: No LocalFav anything exists in shared/feature/entry (grep for localFav/LocalFav returns zero hits). FavoritesPage gates the ENTIRE tab on auth.isLogin and shows a login prompt when logged out — a logged-out user has no favorites at all. There is no 'l' favcat, no local-favorite persistence, and no w
  - Fix: Add a shared LocalFavState (@ObservedV2 holder via AppStorageV2, persisted through the storage layer like other settings snapshots) holding EhGallery[] of locally-favorited galleries. Inject a synthetic 'l' Favcat into FavoritesViewModel.favList (with totNum = local list length). In load(), branch on favcat==='l' to return the local list with no network call. Drop the hard auth gate: when logged o
- [ ] **HIGH** · _architecture_ · Per-favcat tab architecture replaced by a single shared list + chip bar (loses independent scroll/pagination state per favcat)
  - FE: eros_fe renders favorites as a horizontal TAB BAR of favcats (favorite_tabbar_page.dart: LinkScrollBar of favcatList titles inside a pinned/floating SliverPersistentHeader), with a PageView body where EACH favcat is a separate FavoriteSubPage (AutomaticKeepAliveClientMixin, wantKeepAlive=true) backed by its OWN FavoriteSubListController (Get.lazyPut per favId in favorite_tabbar_controller.onInit, 
  - NextE: FavoritesPage is ONE PullRefreshListScaffold with a single FavoritesViewModel and a single dataSource. Switching favcat calls selectFavcat() which CLEARS dataSource (setData([])) and refetches — every favcat switch loses the previously-loaded rows and resets scroll to top. There is no per-favcat sta
  - Fix: Adopt V2Next's HDS tab pattern (HdsTabs / Tabs from @kit.UIDesignKit as used in the M1 nav shell) with one TabContent per favcat, each rendering its own PullRefreshListScaffold bound to a per-favcat sub-viewmodel (a Map<favId, FavSubViewModel> mirroring subControllerMap). Persist the last-selected favcat index in an AppStorageV2 holder (favConfig.lastIndex equivalent). Keep tab content alive so sw
- [ ] **HIGH** · _missed-logic_ · Favorites search surface entirely missing (no SearchType.favorite, no sn/st/sf scope toggles)
  - FE: eros_fe gives favorites a dedicated search flow. favorite_tabbar_page.dart nav bar has a search button → NavigatorUtil.goSearchPage(searchType: SearchType.favorite) opening the search page in favorite mode (search_page_controller has SearchType.favorite, title 'Search Favorites'). request.dart getGallery applies favSearchMap when search!=null && isFav (lines 124-126), and AdvanceSearchController.f
  - NextE: FavoritesViewModel.buildQuery always sends search:'' — fav search is hard-wired off. There is no search button on the favorites tab, no favorites search page, no SearchType.favorite, and no sn/st/sf scope toggles. getFavoritesList accepts a `search` param but nothing ever fills it. A user cannot sea
  - Fix: Add a search affordance to the favorites title bar (V2Next puts the search field in the HDS title bar; reuse AppSearchField/the search scaffold). Route to a favorites-scoped search that fills FavoritesQuery.search and reload. Extend FavoritesQuery with the sn/st/sf scope flags and emit them in getFavoritesList (mirror favSearchMap), surfaced as HDS toggle chips (FilterChip) in the search UI.
- [ ] ⏸ **HIGH** · _architecture_ · Add / remove / move-favorite write architecture absent — even the read-side scaffolding is missing
  - FE: eros_fe has a complete favorite write subsystem the audit says should be matched in structure even if writes are deferred. fav_controller.dart: addFav() decides quick-add-to-last-favcat (isFavLongTap + lastFavcat) vs selectToSave (dialog). selectToSave first GETs current state via galleryGetFavorite→galleryGetFavorite parses FavAdd (current favcat selection + existing favNote) to pre-populate the 
  - NextE: NextE has zero favorite-write code anywhere (no galleryAddFavorite, no gallerypopups.php call, no FavAdd model, no favdel, no addFav/delFav, no increase/decrease optimistic count update). EhGallery has favNote/favTitle fields but nothing reads/writes the note. The GalleryDetail page has no working f
  - Fix: Port the read+structure now (the audit's bar): add a FavoriteWriteService in shared/network for galleryGetFavorite (parse current favcat + favNote into a FavAdd model) and galleryAddFavorite/delFav (gated behind the destructive-write boundary). Add an HDS bottom-sheet favcat picker (V2Next AppModalScaffold + GroupedListSection rows with colored hearts + counts + a favNote TextField) reused by both
- [ ] ⏸ **MEDIUM** · _missed-logic_ · Fav note (favNote) never read or editable
  - FE: eros_fe surfaces and edits the per-gallery favorite note. fav_controller.dart binds _favnoteController to a CupertinoTextField in BOTH add dialogs (placeholder 'Favorites note', ehTheme.favnoteTextFieldBackgroundColor), pre-fills it from galleryGetFavorite's favAdd.favNote, and submits it as favnote in galleryAddFavorite. The note round-trips: read existing → edit → POST.
  - NextE: EhGallery.favNote exists as a field and i18n key gallery_fav_note exists, but nothing populates it from the parse, displays it, or offers an editor. There is no favNote read or write path. The note is dead data.
  - Fix: Parse favNote where EH exposes it (favorites row / gallerypopups), display it on the gallery row/detail when present, and include an editable favNote field in the HDS favcat picker sheet (TextField from the shared scaffolds) that submits with the add/move call.
- [x] **MEDIUM** · _ui-native_ · Dedicated favcat selector page implemented; needs FE device comparison before acceptance
  - FE: eros_fe has a dedicated FavoriteSelectorPage (favorite_sel_page.dart) reached via the 'bars' button in the favorites tab bar (favorite_tabbar_page.dart lines 379-399). It's a CupertinoPageScaffold titled 'Favcat' rendering an inset-grouped list (SliverCupertinoListSection.insetGrouped): each favcat is an EhCupertinoListTile with a colored heart leading icon (ThemeColors.favColor[favId]), the favTi
  - NextE: `7322126 feat(favorites): add favcat selector` adds a Favorites title action and `FavoriteSelectorPage` route using HDS `SecondaryListScaffold` + `GroupedListSection` + `ConciseListRow`. Rows show `全部` plus server favcats, per-favcat colored hearts, trailing counts, current selection state, and tap-to-select then return to Favorites.
  - Validation: `scripts/test_favorites_selector_contract.mjs`, V1 inventory, i18n parity, `git diff --check`, official signed Hvigor build, and HarmonyOS device evidence under `/private/tmp/nexte_favorites_selector_evidence/`.
  - Remaining: Android `eros_fe` source was grounded, but FE selector device screenshot was not reached in this run; keep this as implemented / needs FE comparison or controller acceptance, not final accepted parity.
- [ ] **MEDIUM** · _missed-logic_ · FavOrderException re-load logic reimplemented brittly and diverges from eros_fe's actual detection
  - FE: eros_fe detects order mismatch in the parser layer, not by guessing. FavoriteListHttpTransformer.parse (http_transformer.dart) computes isFavoriteOrder(html) (gallery_list_parser.dart inspects the inline_set <option selected> for fav vs posted) and XORs it against the desired order → throws FavOrderException(order: fs_f|fs_p). request.dart getGallery (lines 154-171) then retries with inline_set=<o
  - NextE: FavoritesViewModel.load() reimplements this ad-hoc: it fires getFavoritesList, then if list.favOrder !== this.favOrder fires a SECOND identical getFavoritesList with the SAME buildQuery('') — but the query already carries inline_set=order on the FIRST call (getFavoritesList always appends inline_set
  - Fix: Move order-mismatch + list-display-mode detection into the favorites parser/transformer layer (an EhFavoriteListTransformer that returns a typed 're-order needed' / 'need dm_l' signal), and have getFavoritesList perform the single-shot retry with inline_set set and the cursor stripped, mirroring request.dart. Add the dm_l (display-mode) retry so non-default account list styles parse. Keep the re-l
- [ ] **MEDIUM** · _missed-logic_ · Bidirectional paging + jump-to-page / jump-to-date (seek) navigator missing
  - FE: eros_fe favorites support full navigation: favorite_sublist_controller implements fetchPrevData (prev cursor), fetchMoreData (next), and fetchDataFrom(jump/seek/page) (lines 52-103). tabview_controller tracks nextPage/prevPage/maxPage/nextGid/prevGid and keepPosition (prevGid.isNotEmpty || prevPage>-1) so loading a middle page keeps scroll anchored. The favorites tab bar exposes a jump button (sho
  - NextE: FavoritesViewModel only does forward paging via nextGid (loadMore). There is no prev paging, no jump-to-page, no jump-to-date/seek, no maxPage UI, no keepPosition. NextE's GalleryList model HAS prevGid/prevPage/maxPage/nextPage fields but the favorites VM ignores all of them. A user deep in a large 
  - Fix: Add a jump entry point in the favorites title bar (HDS menu/button) opening an HDS dialog with (a) page input bounded by maxPage and (b) a date picker for seek (yyyy-MM-dd). Wire FavoritesViewModel to consume prevGid/prevPage and support keepPosition (anchor scroll when entering mid-list). Reuse V2Next PagedListScaffold paging affordances rather than hand-rolling.
- [ ] **MEDIUM** · _missed-logic_ · Favcat bar not seeded with the 10 default slots; depends entirely on a successful parse
  - FE: eros_fe pre-seeds the favcat list. favorite_sel_controller._initFavItemBeans loads the saved favcat list from profile, and if empty falls back to EHConst.favList (the 10 default slots 'Favorites 0'..'Favorites 9', const.dart lines 427-437), always inserting a synthetic 'a' (all) at index 0 and 'l' (local) at the end. The list is persisted to Global.profile.user.favcat via ever() so it survives res
  - NextE: FavoritesViewModel.favList starts EMPTY and is only populated by mergeFavList from a successful parse. The 'a' (全部) chip is a hardcoded UI literal in FavcatBar with count -1 (no aggregate count). There is no default 10-slot seed, no persistence of the favcat list across launches, and no computed 'al
  - Fix: Seed favList from a persisted AppStorageV2 favcat snapshot, falling back to the 10 default slots (Favorites 0..9) like EHConst.favList, with a synthetic 'a' (count = sum of slots) and 'l' (local count). Persist the parsed favcat list (names/counts) so it's available before the network returns and survives restart. Compute the 'all' aggregate count for the 全部 chip.
- [ ] **LOW** · _ui-native_ · Sort-order toggle is reachable only in list/simple-list mode, not in grid mode; hand-rolled inline pill instead of HDS
  - FE: eros_fe puts the sort control in the nav bar (favorite_tabbar_page.dart lines 214-242: a sort_down icon with an 'F'/'P' overlay → controller.setOrder → showFavOrder action sheet listing 'Use posted'/'Use favorited' with a check on the current value, persisted as favoritesOrder). It is always present regardless of list layout.
  - NextE: The order toggle lives inside FavcatBar, which in GRID mode is wrapped in a GridItem that is the first grid cell — but FavcatBar's sort pill is a hand-rolled Text pill with manual padding/borderRadius (FavoritesPage lines 196-214), not a V2Next FilterChip/HDS control, and it cycles silently on tap w
  - Fix: Move the sort control into the favorites title bar (HDS) as a single affordance present in all layouts, opening an HDS action sheet/menu listing 'By favorited time' / 'By posted time' with the current selection checked (mirror showFavOrder), and persist the choice in an AppStorageV2 settings holder. Replace the hand-rolled chips/pill with V2Next FilterChip (Chip from @kit.ArkUI) so the bar is nati
- [ ] **LOW** · _ui-native_ · Favcat chips hand-rolled instead of reusing V2Next FilterChip
  - FE: V2Next ships a shared FilterChip component (shared/src/main/ets/components/FilterChip.ets) wrapping the native ArkUI Chip with activated state, brand activated background, round radius, and SMALL size — exactly the selected/unselected pill UX NextE's FavChip reimplements by hand.
  - NextE: FavoritesPage.FavChip is a bespoke Row with manual backgroundColor/borderRadius/padding/font colors and a separate count Text, duplicating chip styling the shared FilterChip already encapsulates. This is hand-rolled UI the doctrine says must be a shared native component.
  - Fix: Replace FavChip with the shared FilterChip (porting it into NextE shared if not present), passing selected = (favcat===id) and the count as part of the label; let the native Chip handle activated styling. This also fixes per-chip touch-target and theming consistency for free.
- [x] **LOW** · _shallow_ · Favorites loadMore now reuses order/display correction and cursor-based continuation
  - FE: eros_fe's fetchMoreData/fetchPrevData go through FavoriteFetchListClient → getGallery, which re-runs the SAME FavOrderException + ListDisplayModeException guards on EVERY page (request.dart 139-171), so deep pages stay in the correct order/display mode. lastComplete auto-triggers loadDataMore when next is non-empty (favorite_sublist_controller 109-120).
  - NextE: `FavoritesViewModel.fetchPageWithOrderSync(next)` is now shared by initial load and `loadMore()`, so a returned `favOrder` mismatch retries the same cursor once on every page fetch. `EhApiService.getFavoritesList()` already centralizes the `dm_l` display-mode retry. `loadMore()` now continues based on next-cursor progress (`nextGid` non-empty and changed), not on whether the page had any newly-appended rows after dedupe.
  - Validation: `scripts/test_favorites_loadmore_order_contract.mjs`, `scripts/test_favorites_search_contract.mjs`, V1 inventory, `git diff --check`, official signed Hvigor build, and HarmonyOS device smoke on `192.168.50.197:12345` with evidence under `/private/tmp/nexte_favorites_loadmore_order/`.

## My Tags / usertags + models & parsers completeness

> NextE's My Tags is a read-only viewer: it parses tagsets + usertags and renders colored chips grouped by namespace, but is missing the ENTIRE edit/management subsystem that is the whole point of eros_fe's mytags pages — no setUserTag (watch/hide/color/weight), no add/delete usertag (slide-to-delete + add-tag search), no create/rename/delete tagset, no tagsuggest-backed new-tag search, no usertag-edit dialog with color picker. Architecturally it also collapses eros_fe's deliberate two-level navigation (tagset LIST page → usertag DETAIL page, with master/detail on large screens) into a single pa

- [ ] ⏸ **HIGH** · _missed-logic_ · Entire usertag EDIT subsystem missing (setusertag: watch/hide/color/weight)
  - FE: eros_fe's whole mytags feature is editing. eh_usertag_page.dart tapUserTagItem() opens EhUserTagEditDialog then calls Api.setUserTag(apikey, apiuid, tagid, tagColor, tagWeight, tagHide, tagWatch) (api.dart:301 method 'setusertag', mapping bool→1/0 and int.parse(tagid/apiuid)). The dialog (eh_usertag_edit_dialog.dart) toggles _watch/_hide with mutual exclusion (watch on ⇒ hide off and vice-versa), 
  - NextE: MyTagsPage.ets is explicitly 'v1 is read-only; edits (apiuid/apikey) later' (header comment). There is no tap handler on TagChip, no edit dialog, no EhApiPhpService setusertag call. EhApiService only has read-only getMyTags. apiuid/apikey are parsed and stored on EhMytags but never used.
  - Fix: Add EhApiPhpService.setUserTag(isEx, apiuid, apikey, tagid, watch, hide, colorHex, weight) posting the 'setusertag' JSON to /api.php. Make TagChip tappable to open a native edit sheet built from V2Next patterns: a Hds bottom-sheet / @CustomDialog with HdsSwitch for watch+hide (enforce mutual exclusion), a weight stepper/text field, a default-color switch, and a native color picker; on confirm call
- [ ] ⏸ **HIGH** · _missed-logic_ · No add / delete usertag (slide-to-delete + tagsuggest search-and-add)
  - FE: eros_fe lets you add and remove usertags. Delete: each row is a Slidable with an end SlidableAction (red trash) → controller.deleteUserTag(title) → request.dart actionDeleteUserTag(usertags:[id]) posting usertag_action=mass + modify_usertags[]. Add: a search mode (magnifying-glass trailing) with a debounced text field that calls Api.tagSuggest (method 'tagsuggest') + the local translate DB, builds
  - NextE: MyTagsPage has neither. No swipe-to-delete on chips, no add-tag flow, no search field, no tagsuggest call, no actionDeleteUserTag/actionNewUserTag in EhApiService/EhApiPhpService.
  - Fix: Render usertags as a slidable native list (List + .swipeAction end-trash) instead of chips so delete is reachable; add EhApiService.deleteUserTag / newUserTag (form-POST to /mytags) and EhApiPhpService.tagSuggest. Put an add-tag entry in the HdsNavDestination title bar that opens a search sheet using AppSearchField + a debounced tagsuggest query, mirroring eros_fe's reSearch.
- [ ] ⏸ **HIGH** · _missed-logic_ · No tagset management (create / rename / delete tagset)
  - FE: eros_fe manages tag SETS. eh_mytags_page.dart has a '+' nav button → EhTagSetEditDialog → crtNewTagset (actionCreatTagSet tagset_action=create). eh_usertag_page.dart trailing has rename (pencil → EhTagSetEditDialog → renameTagSet, tagset_action=rename) and a delete-tagset trash that only appears when controller.canDelete (request.dart actionDeleteTagSet tagset_action=delete; canDelete derived from
  - NextE: MyTagsPage only switches between existing tagsets (TagsetBar pills). No create/rename/delete. The parser/model now carry `EhMytags.canDelete` from `do_tagset_delete()`, so the delete-capability gate is represented, but no tagset write UI/API is implemented.
  - Fix: Add EhApiService createTagSet/renameTagSet/deleteTagSet (form-POST /mytags). Surface create in the My Tags title bar and rename/delete in the per-tagset view (only show delete when canDelete), using a native Hds text-input dialog rather than a hand-built one. Keep destructive delete behind explicit user authorization/test-gallery acceptance.
- [x] **HIGH** · _missed-logic_ · usertag parser drops textColor and fill/default color state — chip contrast/coloring wrong
  - FE: mytags_parser.dart reads THREE colors from the tagpreview style via colorRex.allMatches: textColor = matches[0], borderColor = matches[3] group1, color(fill) = matches[3] group0, plus defaultColor = customColorValue.isEmpty (from the #color input). So eros_fe knows the tag's fill color, its border color AND the readable text color, and whether the user set a custom color at all.
  - NextE: Implemented. `EhUsertag` carries border `color`, `fillColor`, `textColor`, and `defaultColor`; `EhMytagsParser` captures style colors plus each tag's `tagcolor_<id>` input, and `MyTagsPage`/gallery tag color enrichment use parsed colors rather than forcing white text over the border color.
  - Evidence: `scripts/test_mytags_parser_contract.mjs` asserts text/fill fallback and default/custom color parsing; official signed build passed; HarmonyOS device My Tags smoke loaded real tagsets/usertags without crash.
- [ ] **HIGH** · _architecture_ · EhGallery missing ~15 GalleryProvider fields the detail/reader/favorites surfaces need
  - FE: GalleryProvider (gallery_provider.dart) carries colorRating, isRatinged, ratingFallBack, archiverLink, torrentLink, torrents, visible, parent (text) + parentHref, numberOfReviews, favNote, localFav, galleryComment, galleryImages, chapter, lastViewTime, pageOfList, filesize(int)+filesizeText, ratingCount. These back: the personal-vs-community rating distinction (colorRating/isRatinged/ratingFallBac
  - NextE: Partially implemented. Detail parsing/model now carries `favcat`/`favTitle`, `colorRating`/`isRated`, sprite-derived `ratingFallBack`, `archiverLink` (`or` token), `visible`, and parent gid/token/url/display text; `GalleryInfoPage` renders visible/parent/display-rating/archiver rows. Still incomplete: no `torrentLink` field is needed for current torrents flow because `GalleryTorrentsPage` builds `/gallerytorrents.php?gid&t`; `numberOfReviews`, full `torrents` cache, chapter, and several write/local-fav fields remain outside this narrow parser lane.
  - Fix: Continue only for fields with an active user-visible consumer: add `numberOfReviews` when comments/reviews UI needs it, chapter when reader jump/chapter UI is implemented, and write/local-favorite fields only inside their guarded write lanes.
- [x] **HIGH** · _missed-logic_ · Detail parser skips favcat sprite math + favTitle (heart shows wrong state on detail)
  - FE: gallery_detail_parser.dart computes _favCat from #fav div background-position via `(int.parse(catPosition) - 2) ~/ 19` and _favTitle from #favoritelink (only when it has a single text node = actually favorited). This is a documented verify-live magic constant; it tells the detail page which favcat the gallery is in so the favorite heart/label is correct on open.
  - NextE: Implemented. `EhGalleryDetailParser` reads the `#fav` inner sprite Y offset with `(Y - 2) / 19`, reads the sprite title as `favTitle`, and leaves both empty when `#fav` has no inner sprite. The header favorite state is gated on `gallery.favcat`.
  - Evidence: `scripts/test_gallery_detail_parser_contract.mjs` covers favorited/not-favorited synthetic cases plus optional real authed fixture; `scripts/test_detail_header_visual_contract.mjs` locks the header gate/color.
- [ ] **MEDIUM** · _architecture_ · Two-level page structure collapsed into one (tagset list page → usertag page lost)
  - FE: eros_fe is deliberately two screens: EhMyTagsPage shows a SliverCupertinoListSection of TAGSETS (name + count + chevron); tapping one sets currSelected and routes (EHRoutes.userTags, master/detail id:2 on large layout) to EhUserTagsPage which shows that set's usertags. This is an information-hierarchy decision: pick a set, then manage its tags, with the set name as the nav title and per-set action
  - NextE: MyTagsPage is a single page that lists the FIRST/selected set's tags and offers a horizontal pill bar to switch sets inline (only when tagSets.length>1). There is no tagset list page, no per-set nav title, no place to hang per-set actions, and no large-screen master/detail. The 'pick a set' step and
  - Fix: Split into MyTagsPage (a native HDS list of tagsets via ConciseListRow showing name + count + chevron, with a create action) that pushes a UserTagsPage(tagsetId) onto the nav stack (set name as title, rename/delete/add actions in its title bar). This matches eros_fe's hierarchy and gives the actions a home.
- [ ] **MEDIUM** · _shallow_ · EhUsertag model missing translate / weight-as-string / tagWeight semantics
  - FE: EhUsertag (eh_usertag.dart) carries title, tagid, translate, watch, hide, defaultColor, colorCode, borderColor, textColor, tagWeight (String). tagWeight is a String because EH accepts arbitrary weight text and the edit dialog round-trips the raw value; translate holds the localized tag for zh users (controller isTagTranslat shows it as the row subtitle).
  - NextE: EhUsertag has tagId, tag, display, color, weight:number, watched, hidden. weight is a number (loses non-integer/blank EH values and complicates round-trip on save). There is no translate field, so the tag-translation subtitle eros_fe shows (and uses for search matching) cannot be rendered. defaultCo
  - Fix: Add translate:string; keep weight as the raw string for round-trip (parse to number only when sorting). Populate translate via TagTranslationService and show it as the row subtitle (ConciseListRow subtitle) when the zh translate setting is on, matching eros_fe's isTagTranslat.
- [x] ⏸ **MEDIUM** · _missed-logic_ · Comment parser drops vote state, canEdit/canVote, memberId, scoreDetails, UTC→local time
  - FE: parseGalleryComment (gallery_detail_parser.dart) extracts: memberId (showuser=N), vote (+1/-1 from the c4 up/down span styles), canEdit/canVote (onclick contains edit_/vote_), id (c6 id), score (signed), scoreDetails (the 'Base +N, …' breakdown spans in c7), converts the UTC 'Posted on …' to LOCAL time via DateFormat, and builds a linkified element (URLs → <a>). GalleryComment carries all of these
  - NextE: Implemented. `EhGalleryComment` now carries `memberId`, `vote`, `canEdit`, `canVote`, and `scoreDetails`; `EhCommentParser` extracts `showuser`, c4 edit/vote capabilities, c4 up/down style vote state, c7 score-detail spans, and converts EH UTC comment time to device-local `yyyy-MM-dd HH:mm`.
  - Validation: `scripts/test_comment_parser_contract.mjs` covers uploader/scored comments, member id, up/down vote state, edit/vote capability, score details, and local-time conversion. This is parser/data support only; comment vote/edit UI and destructive writes remain separate guarded lanes.
- [x] **MEDIUM** · _missed-logic_ · Image parser only handles unified gt200 <a> layout — gdtm/gdtl sprite + large-thumb layouts dropped
  - FE: parseGalleryImage (gallery_detail_parser.dart) handles THREE layouts: #gdt>div.gdtm (CSS-sprite small thumbs: per-image url+height+width+offSet+ser-from-img-alt), #gdt>div.gdtl (large thumbs: src split on '-' for oriWidth/oriHeight, largeThumb=true), and #gdt>a (new unified). GalleryImage carries ser, largeThumb, offSet, thumbW/H, oriW/H, originImageUrl, showKey, filename.
  - NextE: Implemented `RE_SMALL_PREVIEW`/`parseSmallThumbs` for `gdtm`, `RE_LARGE_PREVIEW`/`parseLargeThumbs` for `gdtl`, dedupe against unified previews, and parsed whole-thumb aspect for non-sprite fallback.
  - Validation: `scripts/test_gallery_detail_parser_contract.mjs` covers unified, `gdtm`, `gdtl`, sprite extents, and whole-image fallback aspect; emulator detail smoke evidence in `/private/tmp/nexte_gallery_image_parser_legacy_evidence/`.
- [ ] **MEDIUM** · _missed-logic_ · usertags not wired into gallery tag coloring / hide-filter (eros_fe TagController)
  - FE: eros_fe doesn't just display usertags — TagController.addAllTags(usertags) folds them into galleryTags so getColorCode() colors detail/list tags with the user's custom colors, and needHide()/hideTags drives client-side hiding of galleries whose tags the user marked hide. eh_mytags_controller.loadData() calls tagController.addAllTags(ehMyTags.usertags) on every load.
  - NextE: NextE parses usertags only to render the My Tags page. There is no equivalent of TagController: the user's custom tag colors are not applied to gallery-detail/list tag chips, and watched/hidden usertags have no effect on the list (no client-side hide of hidden-tagged galleries, no highlight of watch
  - Fix: Add a shared TagState/holder (AppStorageV2) populated from getMyTags that maps namespace:tag → {color, watch, hide}; have GalleryCard/detail tag rendering consult it for custom colors, and the list ViewModel optionally filter/flag galleries whose simpleTags match a hidden/watched usertag — matching eros_fe's needHide/getColorCode.
- [ ] ⏸ **MEDIUM** · _ui-native_ · Hand-rolled chips + pill bar instead of native HDS list/cell scaffolds
  - FE: The quality bar is: render with native HDS + shared scaffolds, take UX from V2Next. V2Next renders list rows via ConciseListRow (HdsListItemCard with prefix image/switch/suffix text+arrow) and uses HdsSwitch/Hds dialogs; eros_fe itself renders usertags as list TILES (icon + color box + title + subtitle) in inset-grouped sections, not free-floating chips.
  - NextE: MyTagsPage hand-builds the tagset switcher as a Scroll(Row) of raw Text pills with manual padding/borderRadius/colors, and each usertag as a bare Text chip with manual border/opacity. It does not use ConciseListRow, GroupedListSection cells, HdsSwitch, or any HDS list affordance, so there is no row 
  - Fix: Render usertags as ConciseListRow rows inside GroupedListSection: leading icon for watched(green check)/hidden(red x)/neither (matching eros_fe's _buildHideIcon), a trailing color box with the weight, subtitle = translate, row tap → edit sheet, swipe → delete. Replace the pill bar with the tagset LIST page (per the page-structure finding). Keep the V2Next ConciseListRow look so it matches the rest
- [x] **LOW** · _shallow_ · tagset name/count parsing diverges from eros_fe (sort + default handling)
  - FE: mytags_parser.dart parses tagsets from the <select>, splits name/(count) via regex, then SORTS tagSets by numeric value ascending so the set order is stable/predictable, and reads the `selected` option. EhMytagSet carries name, tagCount, enable, value.
  - NextE: Implemented. `EhMytagsParser.parseTagSets` sorts parsed tagsets by numeric id and `selectedTagset()` preserves the selected value; `scripts/test_mytags_parser_contract.mjs` covers stable sorted order.
- [ ] **LOW** · _missed-logic_ · weight default 10 vs server value; no per-namespace 1-letter prefix expansion
  - FE: eros_fe TagController.addAllTags expands 1-letter namespace prefixes via EHConst.prefixToNameSpaceMap (e.g. 'a'→'artist') when bucketing usertags by namespace, and treats tagWeight as the server's raw value. Namespacing in eros_fe is prefix-aware.
  - NextE: EhUsertag.namespace() just splits on the first ':' and returns 'misc' otherwise — a usertag stored as 'a:foo' (short prefix) would group under 'a' rather than 'artist', mis-bucketing it vs how eros_fe groups. weight defaults to 10 when absent, which is fine, but the number type (prior finding) loses
  - Fix: Expand short namespace prefixes via the existing EhConstants namespace map in EhUsertag.namespace() (and in the list/detail SimpleTag parsing) so usertags written with short prefixes bucket under their full namespace, matching eros_fe.
