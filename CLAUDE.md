# Claude Code Entry

Claude Code follows the same repository rules as every other agent. Read [AGENTS.md](AGENTS.md) first and
then only the guide routed there for the current task.

This file intentionally contains no duplicate build, architecture, device, state-management, or product
rules. Duplicating those rules here previously allowed the two entrypoints to drift.
