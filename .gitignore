.hvigor/
entry/build/
**/build/
**/.cxx/
**/BuildProfile.ets
oh_modules/
# DevEco-generated ohosTest intermediate; source tests live in entry/src/ohosTest/.
entry/.test/
node_modules/
__pycache__/
*.pyc
*.hap
*.signed.hap
.env.local
.claude/
.cursor/skills/
.opencode/
.idea/
.appanalyzer/
oh-package-lock.json5
local.properties
# Signing config is injected by dev.sh/sign.py at build time, never committed
scripts/dev.env
# Captured EH page fixtures for local parser smoke-checks (site content, not committed).
# The committed contract tests carry an inline synthetic fixture as the hard gate.
scripts/fixtures/
# Local design exploration rejects and raw prompt outputs. Keep accepted assets
# under tracked project paths instead.
assets/app-icons/_scratch/
# Local DevEco/Harmony build profile; may contain signing paths/passwords.
build-profile.local.json5
# Legacy migration backup; public config is build-profile.json5.
build-profile.json5.github

# Local agent incident notes; never commit their contents or per-file index.
docs/agent-guides/incidents/
